on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy to"
        required: true
        type: choice
        options:
          - staging
          - production
        default: "staging"
      run_ui_test:
        description: "Run UI tests before deployment"
        required: true
        type: boolean
        default: false
      run_test:
        description: "Run tests before deployment"
        required: true
        type: boolean
        default: false
jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      build_status: ${{ steps.set-status.outputs.status }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.0
        with:
          repository: ${{ github.repository }}
          ref: ${{github.ref}}
          fetch-depth: 0
      - name: List files in the repository
        run: ls ${{github.workspace}}
      - name: Set an output status
        id: set-status
        run: |
          echo "Setting output status to success"
          echo "status=success" >> $GITHUB_OUTPUT
  test:
    needs: build
    runs-on: ubuntu-latest
    if: ${{ inputs.run_ui_test == true }}
    steps:
      - name: Ui Testing
        run: |
          echo "This the output from build job: ${{ needs.build.outputs.build_status }}"
          echo "Running UI tests... in ${{ inputs.environment }} environment"
